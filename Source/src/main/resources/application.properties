#hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#hibernate.show_sql=false
#hibernate.hbm2ddl.auto=create
#entitymanager.packagesToScan=com.airavat.panya.dao
#db.driver=org.postgresql.Driver
#db.password=pass
#db.url=jdbc:postgresql://localhost:5432/postgres
#db.username=panya
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/panya
spring.datasource.username=panya
spring.datasource.password=password
# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.max-wait=10000
# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.max-active=50
# Validate the connection before borrowing it from the pool.
spring.datasource.test-on-borrow=false

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

server.port=8484

security.basic.authorize-mode=role
security.basic.enabled=false
 # Comma-separated list of paths to secure.
security.basic.path=/**
security.basic.realm=Panya
security.enable-csrf=false
security.filter-order=0
#security.filter-dispatcher-types=ASYNC, FORWARD, INCLUDE, REQUEST
security.headers.cache=true
security.headers.content-type=true
security.headers.frame=true
#security.headers.hsts= # HTTP Strict Transport Security (HSTS) mode (none, domain, all).
#security.headers.xss=true # Enable cross site scripting (XSS) protection.
 # Comma-separated list of paths to exclude from the default secured paths.
#security.ignored=
security.require-ssl=false
 # Session creation policy (always, never, if_required, stateless).
#security.sessions=stateless
security.user.name=userxxx
security.user.password=password
security.user.role=USER

spring.mvc.view.prefix=/views/
spring.mvc.view.suffix=.jsp
spring.thymeleaf.mode=XHTML
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
